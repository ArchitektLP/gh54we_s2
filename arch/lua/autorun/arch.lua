if not arch then arch = {} end

--Config
arch.Version = 1.0
arch.VersionName = "Alpha"

arch.MainFolder = "arch" --Relative to LUA Directory
arch.CoreFolder = "core" --Relative to MainFolder Directory
arch.ModulesFolder = "modules" --Relative to MainFolder Directory
--Config